# Debug artifacts
.playwright-mcp/
.claude/
site-*.png
v2-*.png
v3-*.png
nul

# Dev iteration archive (screenshots + round-1 snapshots) — not for publication
legacy/

# Session handoff / internal working notes — not for publication
SESSION-*-HANDOFF.md
# …except the cross-session handoffs, which are committed on purpose (per user request)
!SESSION-R4-HANDOFF.md
!SESSION-R5-HANDOFF.md

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
